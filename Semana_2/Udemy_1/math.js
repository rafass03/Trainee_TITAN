const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) // Calcula a área do círculo
console.log(area.toFixed(2)) // Exibe a área com duas casas decimais
console.log(typeof Math) // Exibe o tipo da função Math (objeto)